// ============================================================
// test-setup.ts — Global test setup for vitest
// Extends vitest expect() with @testing-library/jest-dom matchers
// so component tests can use toBeInTheDocument(), etc.
// ============================================================
import '@testing-library/jest-dom';
